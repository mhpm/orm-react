// import { create } from 'zustand';

// type StoreState = ModalStore;

// // Combine all slices into a single store
// const useBoundStore = create<StoreState>()((...a) => ({
//   ...createModalSlice(...a),
// }));

// export default useBoundStore;